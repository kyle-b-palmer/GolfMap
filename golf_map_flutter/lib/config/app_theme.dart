import 'package:flutter/material.dart';

/// Shared palette for map overlays and controls.
abstract final class AppTheme {
  static const accentGreen = Color(0xFF4ADE80);
  static const accentGreenDim = Color(0xFF22C55E);
  static const measureBlue = Color(0xFF4A9EFF);
  static const measureBlueBright = Color(0xFF6BB0FF);
  static const panelBg = Color(0xE8121218);
  static const panelBorder = Color(0xFF2A2A32);
  static const textMuted = Color(0xFF9CA3AF);
  static const teeFill = Color(0x66E8D48A);
  static const teeBorder = Color(0xCC8B7355);
}
